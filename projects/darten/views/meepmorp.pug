extends base

block vars
    -var title = "Geheim"

block body
    h1 Account voor wachtwoord voor de site
    form(method="post")
        input(type="hidden", name="_csrf", value=csrfToken)
        input.userName(type="text", name="userName", value="darten", required=true)
        input(type="password", name="password", required=true)
        input(type="submit")
