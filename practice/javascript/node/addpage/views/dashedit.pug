extends base

block vars
  -var title = "Edit data"

block body
  h2 Edit your data here
  p Just do it
  br
  br
  form(method="post")
    input(type="hidden", name="_csrf", value=csrfToken)
    span Edit data:
    input(type="text", name="data", value=user.data)
    br

    input(type="submit")
